{
  "name": "@havoc_zhang/eslint-config-monorepo",
  "version": "0.1.6",
  "private": true,
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "scripts": {
    "lint": "pnpm -r run stub && eslint .",
    "lint:fix": "pnpm -r run stub && eslint . --fix",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "prepare": "pnpm -r run stub",
    "release": "bumpp -r && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@havoc_zhang/eslint-config": "workspace:*",
    "bumpp": "^9.2.0",
    "eslint": "^8.49.0",
    "eslint-plugin-havoc_zhang": "link:./packages/eslint-plugin-havoc_zhang",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  }
}
